#include "main.h"
/**
 * _isupper -prog checks for uppercase character
 * @c: Variable text
 * Return: Always 0.
 */
int _isupper(int c)
{
if (c >= 'A' && C <= 'Z')
{
return (1);
}
else
{
reurn (0);
}
}
